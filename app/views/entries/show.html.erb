<div class="container">
  <div class="width-60">
    <% @page_title = @entry.title %>
    <h1><%= @entry.title %></h1>
    <p class="entry-data"><%= @entry.posted_at.strftime("%Y/%m/%d %H:%M") %></p>

    <div class="entry-contents">
      <%= sanitize(@entry.body) %>
    </div>

    <%= link_to '戻る', entries_path %>
  </div>
</div>
