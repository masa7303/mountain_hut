<div class="sidebar_title gfont text-center">
  <h4>RECENT<h4>
</div>

  <% entries = Entry.order(posted_at: :desc).limit(5) %>
  <ul class="sidebar_txtbox">
      <% entries.each do |entry| %>
        <li><%= link_to entry.title, entry %></li>
      <% end %>
  </ul>
